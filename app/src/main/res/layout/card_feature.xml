<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/card_padding"
    android:gravity="center">

    <!-- Feature icon -->
    <ImageView
        android:id="@+id/ivFeatureIcon"
        android:layout_width="@dimen/card_icon_size_feature"
        android:layout_height="@dimen/card_icon_size_feature"
        app:tint="?attr/colorPrimary"
        tools:src="@drawable/ic_apps"
        android:contentDescription="@null" />

    <!-- Feature label -->
    <TextView
        android:id="@+id/tvFeatureLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textSize="12sp"
        android:textColor="?attr/colorOnSurface"
        android:gravity="center"
        android:maxLines="2"
        tools:text="App Manager" />

</LinearLayout>
